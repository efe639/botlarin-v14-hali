const { EmbedBuilder } = require("discord.js");

module.exports = {
    conf: {
        aliases: [],
        name: "",
        owner: false,
        enabled: true
    },

    run: async (client, message, args, embed, timeout) => {



    }
} 